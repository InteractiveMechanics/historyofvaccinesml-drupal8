{#
/**
 * @file
 * Default theme implementation to display a node.
 *
 * Available variables:
 * - node: The node entity with limited access to object properties and methods.
     Only "getter" methods (method names starting with "get", "has", or "is")
     and a few common methods such as "id" and "label" are available. Calling
     other methods (such as node.delete) will result in an exception.
 * - label: The title of the node.
 * - content: All node items. Use {{ content }} to print them all,
 *   or print a subset such as {{ content.field_example }}. Use
 *   {{ content|without('field_example') }} to temporarily suppress the printing
 *   of a given child element.
 * - author_picture: The node author user entity, rendered using the "compact"
 *   view mode.
 * - metadata: Metadata for this node.
 * - date: Themed creation date field.
 * - author_name: Themed author name field.
 * - url: Direct URL of the current node.
 * - display_submitted: Whether submission information should be displayed.
 * - attributes: HTML attributes for the containing element.
 *   The attributes.class element may contain one or more of the following
 *   classes:
 *   - node: The current template type (also known as a "theming hook").
 *   - node--type-[type]: The current node type. For example, if the node is an
 *     "Article" it would result in "node--type-article". Note that the machine
 *     name will often be in a short form of the human readable label.
 *   - node--view-mode-[view_mode]: The View Mode of the node; for example, a
 *     teaser would result in: "node--view-mode-teaser", and
 *     full: "node--view-mode-full".
 *   The following are controlled through the node publishing options.
 *   - node--promoted: Appears on nodes promoted to the front page.
 *   - node--sticky: Appears on nodes ordered above other non-sticky nodes in
 *     teaser listings.
 *   - node--unpublished: Appears on unpublished nodes visible only to site
 *     admins.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 * - content_attributes: Same as attributes, except applied to the main
 *   content tag that appears in the template.
 * - author_attributes: Same as attributes, except applied to the author of
 *   the node tag that appears in the template.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 * - view_mode: View mode; for example, "teaser" or "full".
 * - teaser: Flag for the teaser state. Will be true if view_mode is 'teaser'.
 * - page: Flag for the full page state. Will be true if view_mode is 'full'.
 * - readmore: Flag for more state. Will be true if the teaser content of the
 *   node cannot hold the main body content.
 * - logged_in: Flag for authenticated user status. Will be true when the
 *   current user is a logged-in member.
 * - is_admin: Flag for admin user status. Will be true when the current user
 *   is an administrator.
 *
 * @see template_preprocess_node()
 *
 * @todo Remove the id attribute (or make it a class), because if that gets
 *   rendered twice on a page this is invalid CSS for example: two lists
 *   in different view modes.
 *
 * @ingroup themeable
 */
#}
<script>
    {% if content.field_related_media[0] %}
        var data = [];
        {% for key, related_media in content.field_related_media if key|first != '#' %}

            {% if content.field_related_media[key]['#node'].field_orig_filename[0].value|length < 2 %}
				{% set orig_filename = '00000' ~ content.field_related_media[key]['#node'].field_orig_filename[0].value %}

            {% elseif content.field_related_media[key]['#node'].field_orig_filename[0].value|length < 3 %}
                {% set orig_filename = '0000' ~ content.field_related_media[key]['#node'].field_orig_filename[0].value %}

            {% elseif content.field_related_media[key]['#node'].field_orig_filename[0].value|length < 4 %}
                {% set orig_filename = '000' ~ content.field_related_media[key]['#node'].field_orig_filename[0].value %}

			{% else %}
				{% set orig_filename = content.field_related_media[key]['#node'].field_orig_filename[0].value %}
			{% endif %}

            {% if content.field_related_media[key]['#node'].field_orig_filename[0].value %}
                {% if content.field_related_media[key]['#node'].field_taxonomy.target_id == 36 %}
                    data["{{ orig_filename }}"] = {
                        root: "{{ orig_filename }}",
                        title: "{{ content.field_related_media[key]['#node'].title.value }}",
                        description: "{{ content.field_related_media[key]['#node'].body[0].value|e('js') }}",
                        caption: "{{ content.field_related_media[key]['#node'].field_caption[0].value|striptags }}",
                        creator: "{{ content.field_related_media[key]['#node'].field_creator[0].title[0].value }}"
                    };
                {% endif %}
            {% endif %}
        {% endfor %}
    {% endif %}
</script>

<div class="article-node">
	<div class="row">
		<div class="col-md-3 col-sm-12">
		
			<div class="panel-group" id="accordion">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <a data-toggle="collapse" data-parent="#accordion" href="#collapseOne">
                                {% if content.langcode['#markup'] == "Español" %}
                                    La ciencia de las vacunas
                                {% else %}
                                    Vaccine Science
                                {% endif %}
                                <span class="pull-right" style="font-size:10px;">[<span class="glyphicon glyphicon-minus" style="top:2px;"></span>]</span>
                            </a>
                        </h4>
                    </div>
                    <div id="collapseOne" class="panel-collapse collapse">
                        <div class="panel-body">
                        
                        	<ul class="list-unstyled">
                        		{{vaccine_science_articles}}
                        	</ul>
                        </div>
                    </div>
                </div>
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <a data-toggle="collapse" data-parent="#accordion" href="#collapseTwo">
                                {% if content.langcode['#markup'] == "Español" %}
                                    La historia y la sociedad
                                {% else %}
                                    History and Society
                                {% endif %}
                            	<span class="pull-right" style="font-size:10px;">[<span class="glyphicon glyphicon-minus" style="top:2px;"></span>]</span>
                            </a>
                        </h4>
                    </div>
                    <div id="collapseTwo" class="panel-collapse collapse">
                        <div class="panel-body">
                            
                            <ul class="list-unstyled">
                        		{{ history_society_articles }}
                        	</ul>
                            
                        </div>
                    </div>
                </div>
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <a data-toggle="collapse" data-parent="#accordion" href="#collapseThree">
                                {% if content.langcode['#markup'] == "Español" %}
                                    Información sobre las vacunas
                                {% else %}
                                    Vaccine Information
                                {% endif %}
                            	<span class="pull-right" style="font-size:10px;">[<span class="glyphicon glyphicon-minus" style="top:2px;"></span>]</span>
                            </a>
                        </h4>
                    </div>
                    <div id="collapseThree" class="panel-collapse collapse">
                        <div class="panel-body">
                        	<ul class="list-unstyled">
                        		{{vaccine_information_articles}}
                        	</ul>
                        </div>
                    </div>
                </div>
                
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <a data-toggle="collapse" data-parent="#accordion" href="#collapseFour">
                                {% if content.langcode['#markup'] == "Español" %}
                                    Enfermedades que se pueden prevenir con las vacunas
                                {% else %}
                                    Vaccine-Preventable Diseases
                                {% endif %}
                            	<span class="pull-right" style="font-size:10px;">[<span class="glyphicon glyphicon-minus" style="top:2px;"></span>]</span>
                            </a>
                        </h4>
                    </div>
                    <div id="collapseFour" class="panel-collapse collapse in">
                        <div class="panel-body">
                        	<ul class="list-unstyled">
                        		{{vaccine_preventable_diseases}}
                        	</ul>
                        </div>
                    </div>
                </div>
                
                 <div class="panel panel-default">
                    <div class="panel-heading">
                        <h4 class="panel-title">
                            <a data-toggle="collapse" data-parent="#accordion" href="#collapseFive">
                                {% if content.langcode['#markup'] == "Español" %}
                                    Preguntas comunes
                                {% else %}
                                    Common Questions
                                {% endif %}
                            	<span class="pull-right" style="font-size:10px;">[<span class="glyphicon glyphicon-minus" style="top:2px;"></span>]</span>
                            </a>
                        </h4>
                    </div>
                    <div id="collapseFive" class="panel-collapse collapse">
                        <div class="panel-body">
                            <ul class="list-unstyled">
                        		{{ common_questions_articles }}
                        	</ul>
                        </div>
                    </div>
                </div>
                
            </div>
            
            <div class="sidebar">
            	
            	{% if content.field_rel_articles[0] %}
					<div class="sidebar-item">
					
						{% if content.langcode['#markup'] == "Español" %}
							<h5>Artículos Relacionadas</h5>
						{% else %}
							<h5>Related Articles</h5>
						{% endif %}
						
						<ul>
							{% for key, activities in content.field_rel_articles if key|first != '#' %}
								<li>
									<div class="sidebar-blurb">
										<p class="title">
											<a href="{{ path('entity.node.canonical', {'node': activities['#node'].id}) }}">{{ activities['#node'].title.value }}</a>
										</p>
										
										{% if activities['#node'].field_related_media[0] %}
										
											
		
											{% if activities['#node'].field_related_media[0].entity.field_orig_filename.value|length < 4 %}
												{% set orig_filename = '000' ~ activities['#node'].field_related_media[0].entity.field_orig_filename.value %}
											{% else %}
												{% set orig_filename = activities['#node'].field_related_media[0].entity.field_orig_filename.value %}
											{% endif %}
											
										{% endif %}
										
										<p>
											<img style="float:left; margin-right:15px; margin-bottom:5px; width:65px;" 
											src="http://media.historyofvaccines.org/images/{{ orig_filename }}_265.jpg">
											{{ activities['#node'].field_synopsis.value|raw   }}
											<br style="clear:both;" />
										</p>
									</div>
								</li>
							{% endfor %}
						</ul>
					</div>
				{% endif %}	
				
				{% if content.field_rel_blog_posts[0] %}
					<div class="sidebar-item">
						{% if content.langcode['#markup'] == "Español" %}
							<h5>Artículos Relacionados Blog</h5>
						{% else %}
							<h5>Related Blog Posts</h5>
						{% endif %}
						
						<ul>
							{% for key, activities in content.field_rel_blog_posts if key|first != '#' %}
								<li>
									<div class="sidebar-blurb">
										<p class="date" style="margin-bottom:2px;">{{ activities['#node'].created.value|date("m/d/Y") }}</p>
										<p class="title">
											<a href="{{ path('entity.node.canonical', {'node': activities['#node'].id}) }}">{{ activities['#node'].title.value }}</a>
										</p>
										<br style="clear:both;" />
									</div>
								</li>
							{% endfor %}
						</ul>
					</div>
				{% endif %}	
				
				{% if content.field_rel_items[0] %}
					<div class="sidebar-item">
						{% if content.langcode['#markup'] == "Español" %}
							<h5>Enlaces Relacionados</h5>
						{% else %}
							<h5>Related Links</h5>
						{% endif %}
						
						<ul>
							{% for key, activities in content.field_rel_items if key|first != '#' %}
								<li>
									<div class="sidebar-blurb">
										<p class="title">
											<a href="{{ path('entity.node.canonical', {'node': activities['#node'].id}) }}">{{ activities['#node'].title.value }}</a>
										</p>
					
										<p>
											{{ activities['#node'].field_synopsis.value|raw   }}
											<br style="clear:both;" />
										</p>
									</div>
								</li>
							{% endfor %}
						</ul>
					</div>
				{% endif %}	
				
				
				{% if content.field_rel_quiz_question[0] %}
					<div class="sidebar-item quiz-item">
						
						<div class="header">
							<h4>{% trans %}What do you think?{% endtrans %}</h4>
						</div>
						
						<div class="questions">
							<p>
								{{ content.field_rel_quiz_question[0]['#node'].field_question[0].value }}
							</p>
						</div>
						
						<div class="choices" data-answer="{{content.field_rel_quiz_question[0]['#node'].field_answer.value}}">
							<ul>
								{% for answer in content.field_rel_quiz_question[0]['#node'].field_choice %}
								
									<li>
										{{ answer.value }} 
									</li>
									
								{% endfor %}
							</ul>
						</div>
						
						<div class="more-info">
						
							<span class="answer">Correct</span>
							{{ content.field_rel_quiz_question[0]['#node'].body[0].value|raw }}
						</div>
						
					</div>
			
				{% endif %}
				
            </div>
		
	
		</div>
	
		<div class="col-md-9 col-sm-12">			
			<div class="article">
                {{ languageswitcher }}

				<h1>{{ label }}</h1>
				
				<div class="body">
					
				<div class="body-mediaitem">
					{% if content.field_related_media[0] %}
		
						{% if content.field_related_media[0]['#node'].field_orig_filename[0].value|length < 2 %}
            				{% set orig_filename = '00000' ~ content.field_related_media[0]['#node'].field_orig_filename[0].value %}

                        {% elseif content.field_related_media[0]['#node'].field_orig_filename[0].value|length < 3 %}
                            {% set orig_filename = '0000' ~ content.field_related_media[0]['#node'].field_orig_filename[0].value %}

                        {% elseif content.field_related_media[0]['#node'].field_orig_filename[0].value|length < 4 %}
                            {% set orig_filename = '000' ~ content.field_related_media[0]['#node'].field_orig_filename[0].value %}

                        {% else %}
							{% set orig_filename = content.field_related_media[0]['#node'].field_orig_filename[0].value %}
						{% endif %}

                        <div class="gallery-image-container">
                            <div class="image {% if content.field_related_media[0]['#node'].field_taxonomy.target_id == 37 %}hidden{% endif %}">
                                {% if content.field_related_media[0]['#node'].field_taxonomy.target_id %}
                                <div 
                                    class="show-modal" 
                                    data-filename="{{ orig_filename }}"
                                    >+</div>
                                {% endif %}
                                <img src="http://media.historyofvaccines.org/images/{{ orig_filename }}_265.jpg" alt="{{ content.field_related_media[0]['#node'].title.value }}" />
                            </div>
                            {% if content.field_related_media[0]['#node'].field_taxonomy.target_id %}
                                <div class="video {% if content.field_related_media[0]['#node'].field_taxonomy.target_id == 36 %}hidden{% endif %}">
                                    <video src="http://media.historyofvaccines.org/mobile/video/320/{{ orig_filename }}.mp4" poster="http://media.historyofvaccines.org/mobile/video/320/{{ orig_filename }}.jpg" controls="controls" autoplay="true" width="100%"></video>
                                </div>
                            {% endif %}
                        </div>
						<p><small>{{ content.field_related_media[0]['#node'].field_caption.value }} <br /><b>{{ content.field_related_media[0]['#node'].field_copyright.value }}</b></small></p>

					{% endif %}
                    {% if content.field_related_media[1] %}
                        <div id="body-mediaitem-switcher">
                        {% for key, related_media in content.field_related_media[1:] if key|first != '#' %}

                            {% if content.field_related_media[key]['#node'].field_orig_filename[0].value|length < 2 %}
                				{% set filename = '00000' ~ content.field_related_media[key]['#node'].field_orig_filename[0].value %}

                            {% elseif content.field_related_media[key]['#node'].field_orig_filename[0].value|length < 3 %}
                                {% set filename = '0000' ~ content.field_related_media[key]['#node'].field_orig_filename[0].value %}

                            {% elseif content.field_related_media[key]['#node'].field_orig_filename[0].value|length < 4 %}
                                {% set filename = '000' ~ content.field_related_media[key]['#node'].field_orig_filename[0].value %}

    						{% else %}
    							{% set filename = content.field_related_media[key]['#node'].field_orig_filename[0].value %}
    						{% endif %}
                            
                            {% if content.field_related_media[key]['#node'].field_taxonomy.target_id == 36 %}
                                <div class="body-mediaitem-small" data-filename="{{ filename }}" data-type="image" data-caption="{{ content.field_related_media[key]['#node'].field_caption.value }}">
    						        <img src="http://media.historyofvaccines.org/images/{{ filename }}_265.jpg" alt="{{ content.field_related_media[key]['#node'].title.value }}" />
                                </div>
                            {% elseif content.field_related_media[key]['#node'].field_taxonomy.target_id == 37 %}
                                <div class="body-mediaitem-small" data-filename="{{ filename }}" data-type="video" data-caption="{{ content.field_related_media[key]['#node'].field_caption.value }}">
    						        <img src="http://media.historyofvaccines.org/images/{{ filename }}_265.jpg" alt="{{ content.field_related_media[key]['#node'].title.value }}" />
                                </div>
                            {% endif %}
                        {% endfor %}
                        </div>
					{% endif %}
				</div>
				
				{{ node.body[0].value|raw  }}
				
				</div>
				
				{% if content.field_related_timeline_entries[0] %}
					{% for key, entry in content.field_related_timeline_entries if key|first != '#' %}
						<div class="timeline-entry border-box">
							<p class="date">Timeline Entry: {{ entry['#node'].created.value|date("m/d/Y") }}</p>
						
							<p class="title">{{ entry['#node'].title.value }}</p>
						
							{{ entry['#node'].body.value|raw }}
						
							<a href="javascript: void(0);" class="btn btn-green">See this item in the timeline</a>
						</div>
					{% endfor %}
				{% endif %}
				
				{% if content.field_rel_assessment[0] %}
					<div class="assessment border-box">
						<h2>Assessment Questions</h2>
						
						{% for key, assessment in content.field_rel_assessment if key|first != '#' %}
						
						
							<div class="question" data-answer="{{assessment['#node'].field_answer.value}}">
								<p>
									<strong>{{ assessment['#node'].field_question.value }} </strong>
								</p>
								<ul>
									{% for answer in assessment['#node'].field_choice %}
									
										<li>
											{{ answer.value }} 
										</li>
										
									{% endfor %}
								</ul>
							</div>
							
						{% endfor %}
						
						
					</div>
				{% endif %}
			</div>
		
		</div>
	</div>
</div>


<script type="text/javascript">
	
	$(document).ready(function(){
		$('.question ul li').click(function(){
			
			if(!$(this).parent().parent().hasClass('answered')) {
			
				var answer = $(this).parent().parent().data('answer');
				var n = $(this).index();
				
				var chr = String.fromCharCode(97 + n);
				
				if(chr == answer) {
					$(this).addClass('correct');
				} else {
					$(this).addClass('incorrect');
					
					var correctIndex = answer.charCodeAt(0) - 97;
					var value = $(this).parent().parent().find('ul > li:eq(' + correctIndex +')').text();
					$(this).parent().parent().find('ul > li:eq(' + correctIndex +')').text(value + ' (correct)');
					$(this).parent().parent().find('ul > li:eq(' + correctIndex +')').addClass('correct');
				}
				$(this).parent().parent().addClass('answered')
			}
		});
		
		$('.choices ul li').click(function(){
			if(!$(this).parent().parent().hasClass('answered')) {
			
				var answer = $(this).parent().parent().data('answer');
				var n = $(this).index();
				
				var chr = String.fromCharCode(97 + n);
				
				if(chr == answer) {
					$('span.answer').text('Correct');
				} else {
					$('span.answer').text('Incorrect');
				}
				$(this).parent().parent().addClass('answered');
				
				$('.more-info').show();
			}
			
		});
		
	});
	
</script>